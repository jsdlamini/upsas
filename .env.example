# Generate secrets locally: openssl rand -base64 48
POSTGRES_USER=upsas
POSTGRES_PASSWORD=
POSTGRES_DB=upsas
DATABASE_URL=postgresql://upsas:CHANGE_ME@db:5432/upsas
SESSION_SECRET=
TOTP_ENC_KEY=
PASSWORD_BLOCKLIST_PATH=./data/password-blocklist.txt
SMTP_HOST=mail.uniswa.sz
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
TZ=Africa/Mbabane
# Deliberately absent: OAUTH_*, SSO_*, LDAP_*, HIBP_API_KEY, any hosted mail or
# scanning provider. No external service participates in verification.

# Resend email (https://resend.com). Empty = console-stub fallback.
RESEND_API_KEY=
RESEND_FROM=UPSAS <onboarding@resend.dev>
